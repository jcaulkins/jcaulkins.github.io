name: justdothins

on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
        type: choice
        options:
        - info
        - warning
        - debug



jobs:

  build:

    runs-on: ubuntu-latest

    steps:

  #  - uses: actions/checkout@v3

    - run: cwd >> log3.txt
